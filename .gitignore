# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env — NEVER commit secrets
.env
.env.*
!env.example

# drizzle (generated migrations are kept; local journal artifacts are not)
# (keep ./drizzle/*.sql migrations under version control)

# misc
.DS_Store
*.pem
/coverage

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# typescript
*.tsbuildinfo

# editor / os
.vscode/*
!.vscode/extensions.json
.idea
Thumbs.db

# music catalog — real manifest carries licence refs, not committed
scripts/music-manifest.json

# Next.js 16 auto-generated agent rules (regenerated on dev boot)
AGENTS.md
/CLAUDE.md

# Local Insta360 sample(s) for reprojection calibration — do not commit/deploy
public/insta360/
